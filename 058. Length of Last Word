public class Solution {
    public int lengthOfLastWord(String s) {
        String []sts = s.split(" +");
        int len = sts.length;
        if(len<1) return 0;
        return sts[len-1].length();
    }
}
